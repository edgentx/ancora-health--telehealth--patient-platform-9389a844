# Keep the frontend build context small and reproducible: never ship installed
# deps or prior build output into the image build (they are regenerated by
# `npm ci` / `npm run build`), and never leak local env files.
node_modules
.next
out
build
npm-debug.log*
.env
.env.local
.env*.local
.git
.DS_Store
*.tsbuildinfo
