import { ProviderDiscovery } from '@/components/patient/provider-discovery';

/** Patient provider discovery + booking (`/providers`). */
export default function PatientProvidersPage() {
  return <ProviderDiscovery />;
}
