import { IntakeForm } from '@/components/patient/intake-form';

/** Patient health profile / pre-visit intake (`/health-profile`). */
export default function PatientHealthProfilePage() {
  return <IntakeForm />;
}
