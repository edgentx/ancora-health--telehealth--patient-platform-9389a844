import { PatientVisits } from '@/components/patient/patient-visits';

/** Patient appointments (`/appointments`) — upcoming and past visits. */
export default function PatientAppointmentsPage() {
  return <PatientVisits />;
}
