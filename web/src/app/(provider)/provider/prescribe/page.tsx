import { PrescriptionComposer } from '@/components/provider/prescription-composer';

/** Provider e-prescribing composer (`/provider/prescribe`). */
export default function ProviderPrescribePage() {
  return <PrescriptionComposer />;
}
