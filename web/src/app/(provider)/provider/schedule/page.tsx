import { ProviderScheduleView } from '@/components/provider/provider-schedule';

/** Provider schedule (`/provider/schedule`). */
export default function ProviderSchedulePage() {
  return <ProviderScheduleView />;
}
