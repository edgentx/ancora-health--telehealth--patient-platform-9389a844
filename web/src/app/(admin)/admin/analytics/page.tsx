import { AdminAnalytics } from '@/components/admin/admin-analytics';

/** Clinic admin analytics + directory (`/admin/analytics`). */
export default function AdminAnalyticsPage() {
  return <AdminAnalytics />;
}
